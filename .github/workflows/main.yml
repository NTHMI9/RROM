name: VH-OS
on: 
  workflow_dispatch:
  issues:
    types:
      - opened
      - reopened
permissions: write-all
jobs:
  build:
    name: 'Xem quá trình'
    if: github.repository == 'chamchamfy/RROM' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-22.04
    steps:
      - name: 'Chat bot'
        uses: actions/stale@v8
        with:
          only-labels: "Build"
          stale-issue-label: "Wait"
          stale-issue-message: "Đang trong quá trình xây dựng, vui lòng chờ...<br/><br/>Sau khi xong link sẽ được gửi vào bài viết này.<br/><br/>Hoặc xem quá trình xây dựng 📱[Actions](https://github.com/chamchamfy/RROM/actions/runs/$GITHUB_RUN_ID)"
          days-before-stale: 0
          repo-token: ${{ secrets.GITHUB_TOKEN }}
      - name: Nạp dữ liệu
        uses: actions/checkout@v3
      - name: 'Tải về'
        run: |
          # Taive          
          bash .github/Taive.sh
      - name: 'Xem build ROM'
        run: |
          # CC          
          bash .github/Chay.sh
      - name: Upload
        run: |
          # kakathic         
          bash .github/Upload.sh
      - name: Dán Thẻ
        uses: actions-cool/issues-helper@v3.5.2
        with:
          actions: 'add-labels'
          labels: "${{ env.THETB }}"
          token: ${{ secrets.GITHUB_TOKEN }}
          issue-number: ${{ env.NUMBERI }}
      - name: Dán Link
        uses: actions-cool/issues-helper@v3.5.2
        with:
          actions: 'close-issues'
          token: ${{ secrets.GITHUB_TOKEN }}
          issue-number: ${{ env.NUMBERI }}
          labels: "${{ env.THETB }}"
          body: "${{ env.THATBAI }}"   



          
